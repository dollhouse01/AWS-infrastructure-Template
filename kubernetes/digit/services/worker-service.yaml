apiVersion: v1
kind: Service
metadata:
  name: digit-hcm-worker
  namespace: digit-hcm
  labels:
    app: digit-hcm
    tier: worker
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
  selector:
    app: digit-hcm
    tier: worker